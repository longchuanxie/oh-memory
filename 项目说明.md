# oh-memory — 项目说明

## 项目概述

oh-memory 是一个基于 LLM（大语言模型）的个人知识库系统，采用增量式 wiki 构建方式。

与传统 RAG（检索-生成）模式不同，oh-memory 会持续构建和维护一个持久化、可累积的 wiki。LLM 负责编写和维护 wiki，人类负责策划资源和提出问题。

## 核心设计理念

大多数 LLM 知识工具使用 RAG 模式：上传文件，查询时检索片段，生成答案。每次查询都从零开始，知识不会累积。

oh-memory 采用不同的方式：LLM **增量构建持久化 wiki**——一个结构化的、相互关联的 Markdown 文件集合。当添加新资源时，LLM 会读取资源，提取关键信息，并将其整合到现有 wiki 中——更新实体页面、修订摘要、标记矛盾。知识被一次性编译并保持最新，而不是每次查询都重新推导。

**wiki 是一个持久化、可累积的制品。** 交叉引用已经存在，矛盾已经标记，综合反映已阅读的所有内容。随着每个资源和每个问题的加入，wiki 会变得越来越丰富。

## 整体架构

系统采用三层架构：

```
┌─────────────────────────────────────────┐
│           模式层 (Schema Layer)          │
│  AGENTS.md — 定义 LLM 如何操作 wiki      │
├─────────────────────────────────────────┤
│           Wiki 层 (Markdown)             │
│  LLM 生成的、相互链接的页面              │
│  摘要、实体、概念等                      │
├─────────────────────────────────────────┤
│           原始资源层 (Raw Sources)       │
│  不可变的源文档                          │
│  文章、论文、数据文件                    │
└─────────────────────────────────────────┘
```

### 三层架构说明

- **原始资源层**：用户策划的资源集合，不可变，是真相的来源
- **Wiki 层**：LLM 生成的 Markdown 文件，包括摘要页、实体页、概念页、比较页等，由 LLM 拥有和维护
- **模式层**：`AGENTS.md` 告诉 LLM wiki 的结构和需要遵循的工作流程

## 工作流程

### 1. 摄入流程 (Ingest Flow)

**触发时机**：摄入是由用户主动触发的，不是自动进行的。

当用户想要将新的知识资源纳入知识库时：

1. **用户添加资源**：将文章、论文、数据文件等放入原始资源集合目录
2. **用户指示 LLM 处理**：通过对话告诉 LLM "处理这个资源"或类似指令
3. **LLM 读取资源**，讨论关键要点
4. **LLM 编写摘要页面**，更新索引
5. **LLM 更新相关的实体/概念页面**
6. **LLM 在日志中追加条目**

**为什么不是自动的？**

| 原因 | 说明 |
|------|------|
| **人类策展** | 用户负责决定哪些资源值得纳入知识库 |
| **成本控制** | 不是所有添加的文件都需要立即处理 |
| **质量控制** | 用户可以在摄入前筛选资源的质量 |
| **灵活性** | 用户可以批量处理或逐个处理资源 |

### 2. 查询流程 (Query Flow)

1. 用户提出问题
2. LLM 读取索引以找到相关页面
3. LLM 读取具体页面获取详细信息
4. LLM 综合答案并引用来源
5. 有价值的答案作为新页面存回 wiki

### 3. 检查流程 (Lint Flow)

1. LLM 扫描页面之间的矛盾
2. LLM 识别被新资源取代的过时声明
3. LLM 查找没有入站链接的孤立页面
4. LLM 建议新问题和资源

## 项目上下文协议 (PCP)

oh-memory 使用 PCP (Project Context Protocol) 实现跨会话 AI 上下文持久化。任何 AI 编码工具（OpenCode、Claude Code、Cursor、Trae）都可以从上次会话结束的地方继续工作。

### 三层内存模型

- **L1 — 项目认知** (BOOT.md, PROJECT.md)：会话启动时始终加载，小于 500 token
- **L2 — 会话状态** (WORKING.md, CHECKPOINT.md)：用于活动工作延续
- **L3 — 项目知识** (ARCHITECTURE.md, DECISIONS.md, PATTERNS.md, KNOWLEDGE/)：按任务类型按需加载

### 会话生命周期

**Session 自动创建**：当 AI 工具启动时，AGENTS.md 会指示 LLM 检查 `.ai-context/SESSIONS/WORKING/` 目录，如果没有活动会话文件，LLM 会自动创建一个。

会话文件的生命周期：

1. **TEMP** — 24 小时 TTL，由清理脚本自动删除
2. **WORKING** — 7 天活动期，然后压缩到 ARCHIVED
3. **ARCHIVED** — 长期存储，仅保留关键决策和模式

**归档触发时机**：运行 `pcp cleanup` 命令时，清理脚本会自动将超过 7 天的工作会话压缩并移动到归档目录。

### 会话恢复

如果 CHECKPOINT.md 显示 `status: "in-progress"` 且 `lastUpdate` 超过 2 小时，系统会检测到异常终止，并提供 4 个恢复选项：

1. **继续** — 提交当前更改并从 CHECKPOINT.md 状态恢复
2. **审查** — 显示所有更改 (git diff)，然后让用户决定
3. **丢弃** — 还原所有未提交的更改
4. **新任务** — 暂存更改并重新开始

### 频道机制

- 默认频道是当前 git 分支名称
- 不同频道的会话在 INDEX.md 的"按频道"部分隔离
- 切换分支时，检查该频道上是否存在现有会话

## PCP CLI 工具

项目包含一个 CLI 工具 `pcp-cli`，用于管理 PCP 上下文。

### 支持的 AI 工具

| 工具 | 机制 |
|------|------|
| **OpenCode** | `opencode.json` 指令 + AGENTS.md 中的 `@` 引用 |
| **Claude Code** | 自动读取 AGENTS.md |
| **Trae** | 读取 AGENTS.md + Trae Skills |
| **Cursor** | 自动读取 AGENTS.md |
| **Windsurf** | 读取规则文件 |
| **GitHub Copilot** | 读取 copilot-instructions.md |

### 支持的项目类型

| 语言 | 检测文件 | 技术栈标签 |
|------|---------|-----------|
| Node.js | `package.json` | Node.js + 主要依赖 |
| Python | `pyproject.toml` | Python |
| Rust | `Cargo.toml` | Rust |
| Go | `go.mod` | Go |
| Java (Maven) | `pom.xml` | Java, Maven |
| Java (Gradle) | `build.gradle` / `build.gradle.kts` | Java, Gradle |

### CLI 命令

```bash
npx pcp init              # 初始化 PCP（自动检测项目类型）
npx pcp init -i           # 交互模式
npx pcp init --dry-run    # 预览而不写入文件
npx pcp status            # 检查上下文状态
npx pcp cleanup           # 清理旧会话文件
npx pcp recovery          # 检查中断的会话
npx pcp export            # 导出上下文到归档
npx pcp import <archive>  # 从归档导入上下文
```

## 目录结构

```
oh-memory/
├── .ai-context/              # PCP 上下文文件
│   ├── BOOT.md               # 会话入口点（始终首先加载）
│   ├── PROJECT.md            # 项目概述
│   ├── ARCHITECTURE.md       # 系统架构
│   ├── DECISIONS.md          # 决策记录 (ADR)
│   ├── PATTERNS.md           # 代码模式和约定
│   ├── WORKING.md            # 当前工作状态
│   ├── CHECKPOINT.md         # 会话检查点 (JSON)
│   ├── GLOSSARY.md           # 术语表
│   ├── SESSIONS/             # 会话生命周期存储
│   │   ├── INDEX.md          # 会话索引
│   │   ├── WORKING/          # 活动会话
│   │   ├── ARCHIVED/         # 压缩的过去会话
│   │   └── TEMP/             # 临时文件 (24h TTL)
│   ├── KNOWLEDGE/            # 持久化知识
│   │   ├── domain/           # 领域特定
│   │   └── tech/             # 技术
│   └── scripts/              # 自动化脚本
├── src/                      # pcp-cli 源代码
│   ├── cli.ts                # CLI 入口点
│   ├── commands/             # CLI 命令实现
│   ├── scanner.ts            # 项目类型扫描器
│   ├── renderer.ts           # 模板渲染引擎
│   ├── ide-detector.ts       # IDE 检测器
│   ├── templates/            # 参数化模板
│   └── standalone/           # 独立初始化脚本
├── AGENTS.md                 # AI 代理指令
└── README.md                 # 项目说明
```

## 关键设计原则

- **增量式**：知识随时间累积
- **人类策划资源，LLM 维护 wiki**：分工明确
- **持久化制品**：wiki 构建一次，保持最新
- **零依赖**：纯 Markdown + Git

## 技术栈

- **核心**：Markdown 文件，Git 版本控制
- **查看器**：Obsidian（可选，用于图形视图和浏览）
- **搜索**：qmd（可选，用于大规模混合 BM25/向量搜索）
- **演示文稿**：Marp（可选，用于从 wiki 内容生成幻灯片）
- **AI 集成**：AGENTS.md / CLAUDE.md 模式驱动的 LLM 代理

## 使用建议

- **Obsidian** 是最佳的 wiki 查看器——图形视图、反向链接、搜索
- **Obsidian Web Clipper** 可将网页文章转换为 Markdown 以便快速摄入
- **Marp** 可从 wiki 内容生成幻灯片
- **qmd** 在大规模时添加混合 BM25/向量搜索
- wiki 只是一个 git 仓库的 Markdown 文件——免费获得版本历史、分支和协作能力

## 为什么这有效

维护知识库最繁琐的部分是簿记工作——更新交叉引用、保持摘要最新、注意矛盾。人类放弃 wiki 是因为维护成本增长速度超过价值。LLM 不会感到无聊，可以一次处理 15 个文件，维护成本接近于零。

你的工作：策划资源、指导分析、提出好问题。LLM 完成其他所有工作。

## 许可证

MIT