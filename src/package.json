{
  "name": "pcp-cli",
  "version": "0.1.0",
  "description": "Project Context Protocol — cross-session AI context persistence for any project",
  "bin": {
    "pcp": "./dist/cli.js"
  },
  "files": [
    "dist/",
    "templates/"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "ai",
    "context",
    "llm",
    "memory",
    "session",
    "persistence",
    "agents"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
